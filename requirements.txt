fastapi==0.95.2
uvicorn[standard]==0.22.0
# OpenAI client - pinned to a version tested with httpx below
openai==1.12.0
# HTTPX (required by openai) - pin to avoid incompatibilities
httpx==0.24.0
python-dotenv==1.0.1
# PDF and DOCX parsing
PyPDF2==3.0.1
python-docx==0.8.11

